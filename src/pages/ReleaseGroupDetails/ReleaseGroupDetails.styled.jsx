import styled from 'styled-components';

const ReleaseGroupDetailsStyled = styled.main`
  background-color: ${({ theme }) => theme.background};
  color: ${({ theme }) => theme.foreground};
`;

export default ReleaseGroupDetailsStyled;
